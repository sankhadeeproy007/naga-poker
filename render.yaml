services:
  - type: web
    name: naga-poker-backend
    runtime: node
    rootDir: server
    buildCommand: npm install && npx tsc --skipLibCheck --noEmitOnError false
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
